/*
Component: UserIDContext
Purpose: Stores the logged in users ID in the context for access across applications
*/

// imports
import * as React from "react";
// Exports created context
export const UserContext = React.createContext("User");